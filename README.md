# Subsidy Scoring System

**Кейс 2 — Скоринг сельхозпроизводителей при получении субсидий**
---
## Проблема

Сегодня субсидии на развитие животноводства в Казахстане распределяются по принципу **«первый подал — первый получил»**: финансирование уходит тому, кто раньше собрал документы, пока не закончился бюджет. Реальная эффективность хозяйства, история субсидирования и потенциал к росту — не учитываются.

Министерство сельского хозяйства хочет перейти к **merit-based скорингу** — распределению субсидий на основе данных о производителях.

## Решение

AI-система, которая **ранжирует заявки** по 12 факторам на основе Правил субсидирования (Приказ МСХ РК №108), **объясняет каждый score** (explainability) и **формирует shortlist** для рассмотрения комиссией. Финальное решение остаётся за человеком — система рекомендует, не решает.

### Что делает система

1. Загружает реестр заявок (34 587 записей после очистки) из xlsx
2. Заменяет нормативы из датасета на эталонные из справочника (Приложение 1 к Правилам)
3. Исключает отозванные заявки из выборки (уточнённая целевая переменная)
4. Извлекает 12 признаков из каждой заявки (feature engineering v2)
5. Рассчитывает score 0–100 через весовую формулу (12 факторов, 4 группы)
6. Генерирует текстовое объяснение: какие факторы повлияли и насколько
7. Формирует отсортированный shortlist с рекомендациями

---

## Архитектура

```
┌───────────────────────────────────────────────────────────────┐
│                 SUBSIDY SCORING SYSTEM v2.0                   │
│                                                               │
│  ┌──────────────────────┐      ┌───────────────────────────┐  │
│  │     DATA LAYER       │      │     NORMATIVES LAYER      │  │
│  │  pipeline.py         │◀────│  normatives.py            │  │
│  │  очистка, типизация  │      │  normatives_dict.py       │  │
│  │  замена нормативов   │      │  Приложение 1 к Правилам  │  │
│  └──────────┬───────────┘      └───────────────────────────┘  │
│             │                                                 │
│             ▼                                                 │
│  ┌──────────────────────┐      ┌───────────────────────────┐  │
│  │   FEATURE LAYER      │      │    SCORING ENGINE         │  │
│  │  features.py         │────▶│  scoring.py               │  │
│  │  12 признаков        │      │  12 факторов, 4 группы    │  │
│  │  4 группы метрик     │      │  Score 0–100 + risk level │  │
│  └──────────────────────┘      └────────────┬──────────────┘  │
│                                              │                 │
│             ┌────────────────────────────────┘                 │
│             ▼                                                 │
│  ┌──────────────────────┐                                     │
│  │     API LAYER        │                                     │
│  │  FastAPI v2.0        │                                     │
│  │  /score /rank        │                                     │
│  │  /explain /stats     │                                     │
│  └──────────┬───────────┘                                     │
│             │                                                 │
│             ▼                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │              STREAMLIT DASHBOARD                        │  │
│  │  ┌──────────┐  ┌──────────────┐  ┌──────────────────┐   │  │
│  │  │ Ranking  │  │ Factor       │  │ Region analytics │   │  │
│  │  │ table    │  │ breakdown    │  │ direction stats  │   │  │
│  │  └──────────┘  └──────────────┘  └──────────────────┘   │  │
│  └─────────────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────────────┘
```

---

## Данные

**Основной датасет:** реестр заявок на субсидирование за 2025 год, сформированный системой ИСС (subsidy.plem.kz). Получен 19.03.2026.

| Параметр | Значение |
|----------|----------|
| Записей (до очистки) | 36 651 |
| Записей (после очистки, без «Отозвано») | 34 587 |
| Одобренных | 31 678 |
| Отклонённых | 2 909 |
| Регионов | 19 |
| Направлений | 9 (скотоводство, овцеводство, коневодство и др.) |
| Типов субсидий | 47 |
| Суммы | от 300 до 1.23 млрд тенге |

### Нормативы

Нормативы субсидирования берутся **не из датасета**, а из справочника `src/normatives_dict.py`, составленного по **Приложению 1 к Правилам** (Приказ МСХ РК №108 от 15.03.2019, в ред. приказа от 18.11.2025 №428).

### Целевая переменная

Уточнённая бинарная целевая переменная:

| Класс | Статусы | Действие |
|-------|---------|----------|
| Positive (1) | Исполнена, Одобрена, Сформировано поручение, Получена | Включены |
| Negative (0) | Отклонена | Включены |
| Exclude | Отозвано | Исключены из выборки |

Статус «Отозвано» — инициатива заявителя, а не решение комиссии, поэтому он исключён.

---

## Признаки (features) — 12 метрик

Из данных заявки извлекаются 12 числовых признаков, сгруппированных в 4 группы:

### Группа 1: Нормативное соответствие (26%)

| # | Признак | Вес | Описание |
|---|---------|-----|----------|
| 1 | `normative_match` | 10% | Совпадение норматива заявки с эталонным из Приложения 1 |
| 2 | `amount_normative_integrity` | 8% | Арифметическая корректность: делимость суммы на норматив |
| 3 | `amount_adequacy` | 8% | Близость суммы к медиане для данного типа (z-score) |

### Группа 2: Сроки и бюджет (30%)

| # | Признак | Вес | Описание |
|---|---------|-----|----------|
| 4 | `deadline_compliance` | 8% | Подана ли заявка в допустимое окно (20 января — 20 декабря) |
| 5 | `budget_pressure` | 12% | Степень исчерпания бюджета по (регион, направление) на момент подачи |
| 6 | `queue_position` | 10% | Позиция в очереди по дате подачи внутри (регион, направление, тип) |

### Группа 3: Региональная специфика (29%)

| # | Признак | Вес | Описание |
|---|---------|-----|----------|
| 7 | `region_specialization` | 10% | Доля направления в общем числе заявок региона |
| 8 | `region_direction_approval_rate` | 12% | Одобряемость конкретного направления в конкретном регионе |
| 9 | `akimat_approval_rate` | 7% | Уровень одобрения акимата (МИО) |

### Группа 4: Характеристики заявки (15%)

| # | Признак | Вес | Описание |
|---|---------|-----|----------|
| 10 | `unit_count` | 5% | Количество заявленных единиц (перцентильный ранг внутри типа) |
| 11 | `direction_approval_rate` | 5% | Одобряемость направления |
| 12 | `subsidy_type_approval_rate` | 5% | Одобряемость типа субсидии |

---

## Модель скоринга

### Весовая формула (12 факторов)

Score = Σ (value_i × weight_i × 100), где value_i ∈ [0, 1], сумма весов = 1.0.

Результат:

| Метрика | Значение |
|---------|----------|
| Средний балл | 76.0 |
| Медиана | 76.3 |
| Диапазон | 41.6 — 96.4 |
| Низкий риск (≥70) | 26 436 заявок |
| Средний риск (45–70) | 8 150 заявок |
| Высокий риск (<45) | 1 заявка |

---

## Explainability

Каждый score сопровождается группированным объяснением. Пример вывода:

```
Score: 96.4/100 (Риск: низкий)

✓ Нормативное соответствие: сильная (24.3/26 баллов)
    Соответствие норматива эталону: 100% → +10.0
    Арифметическая корректность суммы: 100% → +8.0
    Адекватность суммы заявки: 79% → +6.3

✓ Сроки и бюджет: сильная (29.8/30 баллов)
    Соблюдение срока подачи: 100% → +8.0
    Бюджетное давление: 100% → +12.0
    Позиция в очереди подачи: 98% → +9.8

✓ Региональная специфика: сильная (28.5/29 баллов)
    Профильность направления для региона: 95% → +9.5
    Одобряемость направления в регионе: 100% → +12.0
    Уровень одобрения акимата: 100% → +7.0

✓ Характеристики заявки: сильная (13.8/15 баллов)
    Количество заявленных единиц: 85% → +4.2
    Одобряемость направления: 92% → +4.6
    Одобряемость типа субсидии: 99% → +5.0
```

В будущем данная система будет дополнена работой с ML-моделью, которая будет использоваться для скоринга заявок на основе рассчитанных признаков.
---

## Структура нынешнего проекта

```
subsidy-gov-scoring/
├── src/
│   ├── __init__.py
│   ├── pipeline.py          # Загрузка xlsx, очистка, замена нормативов
│   ├── features.py          # Feature engineering (12 признаков)
│   ├── scoring.py           # Scoring engine (12 факторов) + explainability
│   ├── normatives.py        # Функции для работы с нормативами
│   ├── normatives_dict.py   # Справочник нормативов (Приложение 1)
│   ├── schemas.py           # Pydantic-схемы запросов и ответов
│   └── api.py               # FastAPI endpoints
├── app/
│   ├── dashboard.py         # Streamlit UI
│   ├── api_client.py        # HTTP-клиент dashboard → API
│   └── style.css            # Стили дашборда
├── data/
│   └── subsidies.xlsx       # Исходные данные
├── output/
│   └── shortlist.csv        # Результаты скоринга
├── main.py                  # Entry point (CLI + сервер)
├── docker-compose.yml       # API + Dashboard
├── Dockerfile.api           # Образ FastAPI
├── Dockerfile.dashboard     # Образ Streamlit
├── requirements.txt
└── README.md
```

---

## Быстрый старт

### Вариант 1: Docker Compose

```bash
# 1. Клонировать репозиторий
git clone https://github.com/vlolodyacpp/subsidy-gov-scoring
cd subsidy-gov-scoring

# 2. Поместить данные в data/subsidies.xlsx

# 3. Запустить
docker compose up --build
```

При последующих запусках (если код не менялся) `--build` не нужен:

```bash
docker compose up        # запуск из кэшированных образов
docker compose up -d     # запуск в фоне
docker compose down      # остановка
docker compose logs -f   # просмотр логов
```

### Вариант 2: Локальный запуск без Docker

```bash
# 1. Клонировать репозиторий
git clone https://github.com/vlolodyacpp/subsidy-gov-scoring
cd subsidy-gov-scoring

# 2. Установить зависимости
pip install -r requirements.txt

# 3. Поместить данные в data/subsidies.xlsx

# 4a. Только CLI-скоринг (shortlist в output/)
python main.py

# 4b. API + Dashboard 
python main.py --serve
streamlit run app/dashboard.py
```

---

## API endpoints

| Метод | Путь | Описание |
|-------|------|----------|
| POST | `/score` | Score одной заявки (норматив подставляется из справочника) |
| POST | `/rank` | Ранжирование списка заявок с фильтрами |
| GET | `/explain/{app_id}` | Детальное объяснение score заявки |
| GET | `/stats` | Агрегированная статистика с фильтрами |
| GET | `/applications` | Список заявок с пагинацией |
| GET | `/applications/{app_id}` | Одна заявка по номеру |
| GET | `/regions` | Справочник регионов со статистикой |
| GET | `/directions` | Справочник направлений со статистикой |
| GET | `/health` | Проверка работоспособности |

### Пример запроса POST `/score`

```json
{
  "region": "СКО",
  "direction": "Субсидирование в мясном и мясо-молочном скотоводстве",
  "subsidy_type": "Заявка на получение субсидий за приобретение племенных быков-производителей мясных и мясо-молочных пород",
  "district": "г. Петропавловск",
  "akimat": "Акимат СКО",
  "amount": 780000,
  "submit_month": 3,
  "submit_day": 15
}
```

Поле `normative` не передаётся — система автоматически определяет его из справочника по `subsidy_type`.

---

## Сценарии использования

**Аналитик МСХ формирует shortlist:** загружает заявки → система рассчитывает scores по 12 факторам → отсортированный shortlist → передаёт комиссии.

**Член комиссии проверяет заявку:** выбирает заявку в дашборде → видит score + разбивку по группам факторов → понимает обоснование → принимает решение.

**Региональное управление анализирует:** видит распределение scores по региону → выявляет слабые направления → корректирует работу.

---

## Зависимости

```
pandas>=2.0              # Обработка данных
numpy                    # Численные вычисления
openpyxl>=3.1            # Чтение xlsx
fastapi>=0.100           # REST API
uvicorn[standard]>=0.23  # ASGI-сервер
pydantic>=2.0            # Валидация данных
httpx>=0.27              # HTTP-клиент (dashboard → API)
streamlit>=1.28          # Dashboard
plotly>=5.15             # Визуализации
catboost>=1.2            # ML-модель (этап 2)
shap>=0.43               # Explainability (этап 2)

```

## Roadmap

| Этап | Срок | Содержание | Статус |
|------|------|------------|--------|
| 1 | 27–29 марта | Pipeline, features, rule-based scoring (5 факторов) | Done |
| 2 | 29–31 марта | Scoring v2: 12 факторов, нормативы из справочника, FastAPI | Done |
| 3 | 1–3 апреля | CatBoost, SHAP, Streamlit dashboard | In progress |
| 4 | 4–5 апреля | UX polish, финальное demo, презентация | Planned |

---
